import 'package:flutter/material.dart';

Widget newText() {
  return Row(
    mainAxisAlignment: MainAxisAlignment.center,
    children: const [Text("Wallpaper",style: TextStyle(color: Colors.white),),
    Text("Hub",style: TextStyle(color: Colors.blue),)],
  );
}
